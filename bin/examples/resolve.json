[{"id":"SMOKE","title":"500 {} shows success","finding":"An HTTP 500 response containing {} resulted in 'Schema applied.' Validate HTTP status and the operation result.",
"fixed":"const res = await fetch(url);\nif (!res.ok) { setMsg(`Couldn't apply the schema (HTTP ${res.status}). Retry.`); return; }\nconst out = await res.json();\nif (out.success !== true) { setMsg(\"Couldn't apply the schema. Retry.\"); return; }\nsetMsg(\"Schema applied.\");\n\nTEST 'shows error on 500 {}' PASS: mocks fetch -> status 500 body {}; asserts text \"Couldn't apply the schema (HTTP 500)\" visible and \"Schema applied.\" absent.",
"original":"const res = await fetch(url);\nconst out = await res.json();\nsetMsg(out.message || (out.error ? \"Couldn't apply the schema.\" : \"Schema applied.\"));"}]
