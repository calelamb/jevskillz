[{"id":"met","criterion":"A failed list refresh preserves the last known script records and shows an error with a Retry button.","evidence":"test('list refresh 500 keeps records', ...): initial list shows 'Organization — Entire site'; mock GET list → 500 {}; after refresh: 'Organization — Entire site' still visible; alert text \"Couldn't refresh applied code (HTTP 500).\"; button 'Retry' visible; clicking Retry re-requests list → 200 → alert gone. PASS"},
 {"id":"partial","criterion":"A failed list refresh preserves the last known script records and shows an error with a Retry button.","evidence":"test('list refresh 500 shows error', ...): mock GET list → 500; expect(screen.getByRole('alert')).toHaveTextContent(\"Couldn't refresh\"). PASS"}]
